-- [[ ST4R OMNI-VISION v25.0 - THE FINAL ENFORCER ]]
-- Features: Ghost X-Ray, Turbo FPS, Physical Grab, Anti-Kick, Anti-AFK
-- Optimization: Mobile-First Zero-Lag Engine

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")
local LocalPlayer = Players.LocalPlayer

-- // 1. TURBO FPS STABILIZER (MAX PERFORMANCE)
local function TurboFPS()
    settings().Rendering.QualityLevel = 1
    Lighting.GlobalShadows = false
    Lighting.FogEnd = 9e9
    
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("BasePart") then
            v.Material = Enum.Material.SmoothPlastic
            v.CastShadow = false
            v.Reflectance = 0
        elseif v:IsA("Decal") or v:IsA("Texture") then
            v.Transparency = 1 
        elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("PostEffect") then
            v.Enabled = false 
        end
    end
end
task.spawn(TurboFPS)

-- // 2. ANTI-KICK & MAGNITUDE BYPASS
local oldNamecall
oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
    local method = getnamecallmethod()
    if self == LocalPlayer and (method:lower() == "kick" or method:lower() == "destroy") then
        return task.wait(9e9) 
    end
    if method == "Magnitude" or method == "magnitude" then return 0.1 end
    return oldNamecall(self, ...)
end)

-- // 3. PHYSICAL GRAB WELDING
local function PhysicalAttach(item)
    local char = LocalPlayer.Character
    if not char then return end
    local hand = char:FindFirstChild("RightHand") or char:FindFirstChild("Right Arm") or char:FindFirstChild("HumanoidRootPart")
    
    if item:IsA("BasePart") and not item:FindFirstChild("ST4R_WELD") then
        item.CanCollide = false
        item.Parent = char
        local weld = Instance.new("Weld", item)
        weld.Name = "ST4R_WELD"
        weld.Part0 = hand
        weld.Part1 = item
        weld.C0 = CFrame.new(0, -1, -1.5) -- Positioned in front of hand
    end
end

-- // 4. GHOST X-RAY ENGINE
local function GhostXRay()
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("BasePart") and (v.Name:lower():find("base") or v.Name:lower():find("wall")) then
            v.Transparency = 0.7
            v.Color = Color3.fromRGB(0, 170, 255) -- Ghost Blue
        end
    end
end

-- // 5. CORE AUTOMATION LOOP
RunService.Heartbeat:Connect(function()
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    local hum = char and char:FindFirstChild("Humanoid")

    if hum then hum.WalkSpeed = 85 end

    if root then
        GhostXRay()
        for _, v in pairs(workspace:GetDescendants()) do
            -- Player ESP (Names)
            if v:IsA("Model") and Players:GetPlayerFromCharacter(v) and v ~= char then
                if not v:FindFirstChild("ST4R_Label") then
                    local b = Instance.new("BillboardGui", v)
                    b.Name = "ST4R_Label"
                    b.AlwaysOnTop = true
                    b.Size = UDim2.new(0, 80, 0, 20)
                    b.StudsOffset = Vector3.new(0, 3, 0)
                    local t = Instance.new("TextLabel", b)
                    t.Size, t.BackgroundTransparency, t.TextColor3 = UDim2.new(1,0,1,0), 1, Color3.new(1,0,0)
                    t.Text, t.Font, t.TextSize = Players:GetPlayerFromCharacter(v).Name, Enum.Font.GothamBold, 10
                end
            end

            -- Universal Physical Steal
            if v:IsA("TouchTransmitter") and v.Parent and v.Parent:IsA("BasePart") then
                local item = v.Parent
                if item.Parent ~= char and (item.Position - root.Position).Magnitude <= 35 then
                    firetouchinterest(root, item, 0)
                    firetouchinterest(root, item, 1)
                    PhysicalAttach(item)
                end
            end
        end
    end
end)

-- MOVEABLE UI STAR
if LocalPlayer.PlayerGui:FindFirstChild("ST4R_V25") then LocalPlayer.PlayerGui.ST4R_V25:Destroy() end
local sg = Instance.new("ScreenGui", LocalPlayer.PlayerGui)
sg.Name = "ST4R_V25"
local b = Instance.new("TextButton", sg)
b.Size, b.Position = UDim2.new(0,35,0,35), UDim2.new(0,10,0.5,-17)
b.Text, b.BackgroundColor3, b.Draggable = "★", Color3.new(0,0,0), true
b.TextColor3, b.Active = Color3.new(1,1,1), true
Instance.new("UICorner", b).CornerRadius = UDim.new(1,0)
local s = Instance.new("UIStroke", b)
s.Thickness = 2
RunService.RenderStepped:Connect(function() s.Color = Color3.fromHSV(tick()%3/3,1,1) end)

print("ST4R v25.0 LOADED - OMNI-VISION ACTIVE")
